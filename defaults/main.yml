---
# ------------------------------------------------------------------------------
# Set the settings of Server vCenter according environment
# ------------------------------------------------------------------------------
vmware_vcenter_host: "10.152.35.35"
vmware_vcenter_datacenter: "WindStream - environments"
vmware_vcenter_cluster: "Developments"

# ------------------------------------------------------------------------------
# Set the hosts configurations
#
# Guest SO Options:
#   rhel: rhel7_64Guest
#   windows2012: windows7Server64Guest
#
# Templates:
#   rhel: Template_rhel-7.3_x64-PACKER-base-{VERSION}
#   windows:
# ------------------------------------------------------------------------------
vmware_hosts:
  - name: "Foreman"
    note: Annotation Example
    vcpus: 2
    memory_size: 2048
    so_guest: "rhel7_64Guest"
    disks_list:
      - size_gb: 12
        datastore: "Developments-01"
      - size_gb: 12
        datastore: "Developments-01"
    networks:
      - name: DEV
        ip: 10.152.20.224
        netmask: 255.255.255.0
        gateway: 10.152.20.1
        device_type: "vmxnet3"
    template_base: "Template_rhel-7.3_x64-PACKER-base-v1.2"
    vm_folder: "/Dev/EvolucaoInfraLabs"
    state: present

# ------------------------------------------------------------------------------
# Set this variable to rename a VM
# ------------------------------------------------------------------------------
vmware_new_vm_name:
  - list_of_name

# ------------------------------------------------------------------------------
# Set this configurations to VM Windows
# ------------------------------------------------------------------------------
vmware_costumizations_windows_autologon:

# ------------------------------------------------------------------------------
# For custom variables
# ------------------------------------------------------------------------------
